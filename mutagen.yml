sync:
    app:
      mode: "two-way-resolved"
      alpha: "./"
      beta: "myhost:/home/vagrant/cakephp"
      ignore:
        vcs: true
        paths:
          - "/node_modules"
          - "/log"
          - "/tmp"